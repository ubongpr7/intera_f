services:
  web:
    image: ubongpr7/facebook_camp_f:latest
    ports:
      - "3000:3000"  
    environment:
      - NODE_ENV=development 
    volumes:
      - .:/app               
      - /app/node_modules    
    command: ["yarn", "dev"] 
    restart: unless-stopped

